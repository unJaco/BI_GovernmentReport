# Browse through all data

```sql df
SELECT * FROM faktentabelle 
```

```sql correlation
SELECT * FROM cor 
```

<DataTable data="{df}" search="true" title="Data" />

<DataTable data="{correlation}" search="true" title="Correlations" />